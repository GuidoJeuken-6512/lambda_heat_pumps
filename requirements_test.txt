-r requirements.txt

# Pinned, and deliberately close to what people run: this brings the Home
# Assistant it is built against, and an unpinned one drifts to whatever was
# current when the environment was made. The suite was passing against an HA
# two releases older than any user had, which is a poor place to find out that
# something was removed.
pytest-homeassistant-custom-component==0.13.355

pytest-timeout
flake8
mypy
