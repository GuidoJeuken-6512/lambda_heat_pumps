name: Deploy Documentation

on:
  push:
    branches:
      - "aus"
    paths:
      - 'docs_src/**'
      - 'mkdocs.yml'
      - '.github/workflows/docs.yml'
  workflow_dispatch:

permissions:
  contents: write
  pages: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      TARGET_BRANCH: ${{ github.ref_name }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-docs.txt

      - name: Build documentation
        run: |
          python -m mkdocs build

      - name: Deploy to `gh-pages` branch (if current branch is gh-pages)
        if: env.TARGET_BRANCH == 'gh-pages'
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./site
          publish_branch: gh-pages
          cname: false

      - name: Publish built site into this branch's `docs/` folder
        if: env.TARGET_BRANCH != 'gh-pages'
        run: |
          echo "Target branch: ${TARGET_BRANCH}"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout "${TARGET_BRANCH}"
          rm -rf docs || true
          mkdir -p docs
          cp -r site/* docs/ || true
          git add -A docs
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore(docs): update generated site for ${TARGET_BRANCH}"
            git push origin "HEAD:${TARGET_BRANCH}"
          fi

